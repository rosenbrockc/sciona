FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml .
COPY ageom/ ageom/
RUN pip install --no-cache-dir ".[visualizer]"
EXPOSE 8080
CMD ["uvicorn", "ageom.visualizer_api:app", "--host", "0.0.0.0", "--port", "8080"]
