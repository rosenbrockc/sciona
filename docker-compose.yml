services:
  memgraph:
    image: memgraph/memgraph-mage
    ports:
      - "7687:7687"
    command: ["--bolt-server-name-for-init=Neo4j/"]
    volumes:
      - memgraph_data:/var/lib/memgraph

  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: ageom_architect
      POSTGRES_USER: ageom
      POSTGRES_PASSWORD: ageom_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

  visualizer:
    build:
      context: .
      dockerfile: Dockerfile.visualizer
    ports:
      - "8080:8080"
    environment:
      AGEOM_MEMGRAPH_URI: bolt://memgraph:7687
      AGEOM_MEMGRAPH_USER: ""
      AGEOM_MEMGRAPH_PASSWORD: ""
    depends_on:
      - memgraph

volumes:
  memgraph_data:
  postgres_data:
