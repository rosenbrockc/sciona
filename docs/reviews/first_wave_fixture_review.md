The independent first-wave graph is an ingested **synthetic parser fixture**, not
another remotely sourced physics derivation. The read-only audit resolves all
four exact-version bindings, verifies all three candidate payloads, and compares
the complete stored snapshot with the pinned checked-in synthetic fixture.

The equations are recognizable Newtonian relations, but the chain does not
encode the nonzero-mass division condition or the kinematic premise
`a(t) = d²x(t)/dt²`. A valid physical interpretation also needs positive constant
mass, net force in an inertial frame, and a twice-differentiable trajectory.
Constant-force implementations would cover only a restricted instance.

Retain this original as draft test provenance. Do not count it as a missing
externally sourced physics implementation or promote it based on fixture
identity. A separately corrected and validated executable derivation remains
possible. This audit changes no catalog state.

Together with the legacy reconciliation, the original physics inventory is
44 remote source families represented twice, plus this one synthetic fixture.
All 44 remote families have approved lower-tier realizations with the explicit
scope and corrections in their respective reviews. This does not claim literal
execution equivalence with every original source step.
