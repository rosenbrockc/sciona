# OpenVaccine Community publication readiness

The candidate is a corrected execution of the DasLab reconstruction. Its intended designation is automated Tier 3 Community. Human review is required only for Tier 1; it is not a prerequisite for this candidate's Tier 3 publication.

The two original intake versions describe the same competition solution but differ from the recovered implementation. They remain draft. A derived publication must retain both as mandatory provenance dependencies and distinguish the recovered TensorFlow shared graph base and four prediction families from the intake descriptions. The twenty-model reconstruction is not the original winning clustered final blend.

The serialized boundary accepts explicit populations, masked targets represented by JSON null, cluster membership and proximity factors, all twenty member splits, and complete supervised/pseudo-label round plans. It requires positive training step counts, an initialization seed, uncertainty controls, reversal decisions, standard-deviation convention and rollback policy. Execution sets process-global random seeds; concurrent isolated reproducibility and stochastic checkpoint replay are not certified. Population construction and the historical training recipe are not inferred.

Validation uses generated synthetic inputs only. The graph execution report is the authoritative full-run evidence for its recorded implementation hashes. Earlier component reports with different hashes apply only to their recorded revisions; they must not be relabeled as current tests. The boundary report records the combined 81-test run.

Before publication:

- Record and validate the runtime dependency/provisioning contract. TensorFlow 2.20.0 and tf-keras 2.20.1 currently run from the isolated review target with legacy Keras selected before TensorFlow import. That target is not a deployment-ready installation. Source features also require the reviewed NumPy and pandas behavior.
- Bind the pinned DasLab source and MIT notice, CONTRAfold-SE engine/build identity, separately licensed EternaFold coefficients, provider source, graph, current implementation and relevant synthetic evidence in the semantic review.
- Resolve stale component evidence explicitly using current tests or documented superseding evidence. Include negative semantic and publication gate checks.
- Validate a transaction rollback and read-only absence, then apply the authorized Tier 3 publication, verify idempotency, and execute the actual served graph.

No historical competition accuracy, independent cross-validation accuracy, Tier 2 community-usage qualification or Tier 1 certification is claimed. Historical checklist above has now progressed through automated semantic/environment review, 13 semantic and six publication rejection checks, successful 53-row rollback, read-only absence, authorized Tier 3 application and zero-row idempotent reapplication. The graph is served; final catalog-retrieved full execution passed and is recorded in competition_openvaccine_publication_verification.json. See competition_openvaccine_worker.md for the provisioned worker contract.
