The DSB reconstruction and its eight provider atoms are now approved and served at automated Tier 3 in the configured local Supabase catalog. A fresh read-only verifier retrieved the published graph and passed the complete independent synthetic source comparison. This is not a production deployment claim.

All eight graph nodes execute from raw world-coordinate training/validation arrays, explicit detector weights and proposal inputs through classifier initialization, consecutive training epochs, validation, checkpoint restoration and raw-volume inference. Profile 3 epochs 49–51 and profile 4 epochs 159–161 match the pinned source exactly for final checkpoint tensors, Torch RNG state, all 16 validation passes and both prediction outputs. Inference includes a nonempty case with 77 retained proposals and an empty case.

The reference uses pinned preparation, segmentation, loader, network, loss, epoch-control, validation, split/combine, decoding, NMS and crop definitions. File/clock/worker IO is replaced with synthetic arrays and explicit RNG/order inputs. Integer-division and Boolean-subtraction adaptations are documented. Voxel annotation preparation has separate source comparisons; the complete training graph comparison exercises the world-coordinate branch.

The publication suite has 108 passing tests: 103 runtime tests and five publication-contract checks. Execution-only call coverage maps all ten stages in the exact live source CDG to the reconstructed implementations; helper calls outside the runner cannot satisfy coverage. Center feature extraction and noisy-OR share CaseNet.forward and have separate numerical source evidence.

Publication completed atomically after a rollback rehearsal. The transaction added 211 catalog rows, preserved exact provider versions and graph source lineage, recorded automated Tier 3 reviews and checked served visibility. A repeated rollback run required zero new rows. The catalog boundary has 18 shared inputs and three outputs, including training metrics; callable defaults are verified in both atom interfaces. The immutable graph snapshot retains its validation-time draft metadata; catalog approval evidence records the subsequent publication decision.

The read-only post-publication report is `competition_dsb_publication_verification.json`; the published version is `56f29a87-40dc-5038-97e8-ab5eb099cfbd`. Served CDGs increased from 69 to 70. Explicit detector checkpoints and preprocessed-frame proposal arrays remain runtime requirements. Synthetic state and computation evidence establish no competition accuracy. The original intake graph and earlier proposed bindings remain draft. Tier 1 human review has not been claimed.
